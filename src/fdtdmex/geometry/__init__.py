"""Geometry: shapes, GDS/STL import, voxelization, and the continuous-epsilon evaluator.

Provides ``chi1p1(point) -> eps`` (scalar permittivity at an arbitrary continuous point) that the
subpixel-smoothing kernel samples, plus binary voxelization for the non-smoothed path. See
docs/subpixel-smoothing.md.

Status: stub.
"""
